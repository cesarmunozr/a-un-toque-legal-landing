# Tasks: fullpage-landing

## Phase 0: Setup [BLOCKING]

- [ ] 0.1 [BLOCKING] Run `npm install motion@^11` — adds Motion One as a production dependency
- [ ] 0.2 [BLOCKING] Run `npm uninstall @fontsource-variable/cormorant-garamond @fontsource-variable/inter`
- [ ] 0.3 [BLOCKING] Rewrite `src/styles/global.css` — 9x `@font-face` for Montserrat 100–900, `@theme` with brand colors + font-sans + breakpoints, base resets, fullpage CSS classes (fp-lock, fp-track, fp-section with 100dvh + env safe-area padding), reduced-motion override
- [ ] 0.4 [BLOCKING] Update `src/layouts/BaseLayout.astro` — remove fontsource imports, add `robots?` + `canonical?` props, add preload hints for Montserrat-Light.otf and Montserrat-Black.otf, remove legacy v1 IntersectionObserver script
- [ ] 0.5 [BLOCKING] Update `src/data/site.ts` — add `ClientLogo` interface, `clientLogos` array (5 entries), `siteMeta` export; keep all existing exports unchanged

## Phase 1: Shared Components [BLOCKING for phases 2+3]

- [ ] 1.1 Create `src/components/shared/Logo.astro` — Props: `variant: "dark" | "light" | "sand"`, `maxWidth?: number` (default 480), `class?: string`; map: dark→`"/logos/ 7.svg"`, light→`"/logos/ 6.svg"`, sand→`"/logos/ 8.svg"` (exact leading space); this is the ONLY file referencing leading-space filenames
- [ ] 1.2 Create `src/components/shared/ContactForm.astro` — 4 fields (Nombre/Teléfono/Email/Consulta textarea); CSS `peer` floating-label technique; inputs: transparent bg, bottom border only `1px solid #807662`, text `#E0E0E0`; labels: Montserrat Light 300 `0.75rem` `#807662`; submit: outlined `#C1B7A7`, hover fills; min 44px touch height on all inputs

## Phase 2: Fullpage Engine + Shell [BLOCKING for phase 3]

- [ ] 2.1 Create `src/scripts/fullpage.ts` — import `{ animate }` from `"motion"` (NOT `"motion/react"`); `FPState` interface (current, total, animating, lastNav); constants EASING=[0.76,0,0.24,1], DURATION=0.7, COOLDOWN=800, SWIPE_MIN=50; `navigate(dir: 1|-1)`: double-gate (animating flag + timestamp cooldown), concurrent outgoing+incoming Motion One animate() calls, `Promise.all([a1.finished, a2.finished]).then(...)` for state flip + aria-hidden + pointerEvents; `updateProgress()`: [data-progress-fill] height formula `((current+1)/total)*100%`, animated 0.5s; `updateCounter()`: [data-counter] textContent, padStart(2,"0") format `"01 / 07"`; `scheduleHintFade()`: setTimeout 3000ms then animate [data-hint] opacity 0; keyboard (ArrowDown/Enter/Space→navigate(1), ArrowUp→navigate(-1)); click listener (skip `a, button, input, textarea, select, [data-no-nav]`); touchstart/touchend both passive:true, skip [data-no-nav], min 50px vertical delta; `init()`: add fp-lock class to html, position all sections translateY(0%/100%), set aria-hidden, call updates+hint+dispatch fp:enter on S01; reduced-motion guard; expose `window.__fpNavigateTo = (i) => { navigate to index i }` for CTA use; DOMContentLoaded gate
- [ ] 2.2 Rewrite `src/pages/index.astro` — import BaseLayout + all 7 fullpage section components; pass `robots="noindex, nofollow"` to BaseLayout; render `<main id="fp-track" class="fp-track">` with S01–S07; outside main: progress bar (fixed right edge, 2px wide, 100dvh, inner `[data-progress-fill]` bg-[#807662]); section counter (fixed top-right, `[data-counter]`, Montserrat Light 300, 11px, `#C1B7A7`, tracking-[0.2em], initial "01 / 07"); `<script>` importing `../scripts/fullpage.ts`; zero legacy imports from sections/

## Phase 3: Fullpage Sections

- [ ] 3.1 Create `src/components/fullpage/S01Intro.astro` — `data-section="0"`, `fp-section bg-[#1E1E1E] flex flex-col items-center justify-center`; `<Logo variant="dark" maxWidth={480} class="w-[70vw] md:w-[280px]" />`; firm name: Montserrat Light 300, 12px, `#C1B7A7`, tracking-[0.2em], uppercase, text "CISTERNAS · LORENZINI · SALEH · SANTIAGO, CHILE"; hint `[data-hint]`: absolute bottom-[calc(env(safe-area-inset-bottom)+2rem)] centered, same style, text "↓ toca para continuar"; `<script>` fp:enter listener (once): animate logo opacity:[0,1] 1.2s delay 0.2s, firm name opacity+translateY 0.6s delay 0.8s
- [ ] 3.2 Create `src/components/fullpage/S02Value.astro` — `data-section="1"`, `fp-section bg-[#E0E0E0] relative flex flex-col items-center justify-center`; top-left absolute `<Logo variant="sand" maxWidth={160} class="absolute top-8 left-8" />`; centered (max-w-[700px]): headline "Resolución legal de primer nivel. Siempre a un toque." Montserrat Black 900 `text-[5rem] sm:text-[2.5rem]` `#1E1E1E` leading-[0.95]; subtext Montserrat Light 300 1.1rem `#807662`; CTA "Consulta directa →" outlined `1px solid #1E1E1E`, hover fill, `data-nav-to="6"` attribute; `<script>`: bind [data-nav-to] click to `window.__fpNavigateTo(6)`; fp:enter listener: stagger headline words opacity+translateY 80ms each, then subtext+CTA fade in
- [ ] 3.3 Create `src/components/fullpage/S03Differentiators.astro` — `data-section="2"`, `fp-section bg-[#FAFAFA] flex items-center justify-center`; import `{ differentiators }` from `../../data/site`; grid `grid-cols-3 md:grid-cols-1 gap-8 px-32 md:px-8`; mobile inner wrapper: `overflow-y: auto; touch-action: pan-y` if content may overflow; each card `[data-card]`: numeral Montserrat Thin 100 `9rem md:5rem` `#C1B7A7`, title Montserrat Bold 700 `1.1rem` `#1E1E1E`, body Montserrat Light 300 `0.9rem` `#807662`; fp:enter: each card `opacity:[0,1]` + `translateY:["30px","0px"]` 800ms stagger 150ms×index
- [ ] 3.4 Create `src/components/fullpage/S04Practice.astro` — `data-section="3"`, `fp-section bg-[#1E1E1E] flex items-center justify-center`; import `{ practiceAreas }` from `../../data/site`; centered max-w-[900px]; `<ul>` with `divide-y divide-[#807662]/30`; each `<li class="group flex items-baseline justify-between gap-4 py-6 cursor-default transition-colors hover:bg-[#807662]/10">`: number Montserrat Light 300 `0.8rem` `#807662`, area Montserrat SemiBold 600 `1.3rem` `#E0E0E0`, description Montserrat Light 300 `0.85rem` `#C1B7A7`, arrow `→` `group-hover:translate-x-[6px] transition-transform duration-300` Montserrat Light 300 `#807662`; fp:enter: each row `translateX:["30px","0px"]` + `opacity:[0,1]` 80ms stagger
- [ ] 3.5 Create `src/components/fullpage/S05Clients.astro` — `data-section="4"`, `fp-section bg-[#E0E0E0] flex flex-col`; import `{ Image } from "astro:assets"` + `{ testimonials, clientLogos }` + all 5 client images from `../../assets/clients/`; top h-1/2: 3-col testimonials (mobile stack + overflow-y:auto); each: `"` Montserrat Black 900 `6rem` `#C1B7A7`, quote Montserrat Light italic `1rem` `#1E1E1E`, name Montserrat SemiBold 600 `0.8rem` tracking-[0.15em] `#1E1E1E`, role Montserrat Light 300 `0.75rem` `#807662`; bottom h-1/2: logo strip with `<Image />` auto-WebP, CSS filter grayscale(1) opacity(0.5) hover grayscale(0) opacity(1) transition-500; caption Montserrat Light 300 `0.7rem` `#807662` centered text "Deporte profesional · Alimentación & FoodTech · Publicidad exterior · Sector público · Medios & Distribución"
- [ ] 3.6 Create `src/components/fullpage/S06Team.astro` — `data-section="5"`, `fp-section bg-[#FAFAFA] flex flex-col items-center justify-center px-8`; import `{ Image }` + `{ team }` + all 5 lawyer images from `../../assets/lawyers/`; headline "Quiénes llevan tu caso." Montserrat Bold 700 `2rem` `#1E1E1E`; desktop: `flex flex-row gap-6`; mobile: `[data-no-nav]` wrapper `flex overflow-x-auto snap-x snap-mandatory`, each card `snap-start shrink-0 w-[80vw]`; each card: `<Image />` with `aspect-square object-cover grayscale hover:grayscale-0 hover:scale-105 transition-all duration-500`, name Montserrat SemiBold 600 `#1E1E1E`, role Montserrat Light 300 `0.8rem` `#807662`; link "Ver perfiles completos → /landing#equipo" Montserrat Light 300 `#807662`
- [ ] 3.7 Create `src/components/fullpage/S07Contact.astro` — `data-section="6"`, `fp-section bg-[#1E1E1E] relative`; import `Logo`, `ContactForm`, `{ contact }` from `../../data/site`; inner `grid grid-cols-2 md:grid-cols-1 h-full`; left pane: `<Logo variant="dark" maxWidth={140} />`, contact block `border-l-2 border-[#807662] pl-6 mt-8` with address/phone/email from contact (Montserrat Light 300 `0.9rem` `#C1B7A7`), italic note "Respondemos en menos de 24 horas hábiles"; right pane: `<ContactForm />`; footer line absolute `bottom-8 left-1/2 -translate-x-1/2` Montserrat Light 300 `10px` `#807662` "© 2026 A un toque legal — Cisternas, Lorenzini & Saleh."

## Phase 4: Landing Page [Parallel to Phase 3]

- [ ] 4.1 Create `src/components/landing/TopBannerBack.astro` — fixed top, full width, height 40px, z-50, bg-[#1E1E1E]; `<a href="/">← Volver a la experiencia</a>` Montserrat Light 300 `0.8rem` `#C1B7A7`; min 44px touch target
- [ ] 4.2 Create `src/components/landing/LHero.astro` — `<h1>` SEO-rich (firm name + Santiago + specialties); subheadline; CTA → `#contacto`; pt-10 to clear fixed banner; brand colors + Montserrat typography
- [ ] 4.3 Create `src/components/landing/LDifferentiators.astro` — import `{ differentiators }`; same 3-card content, standard scrollable grid layout (no fullpage), Montserrat typography
- [ ] 4.4 Create `src/components/landing/LHowWeWork.astro` — import `{ steps }`; 5 steps numbered list; step index, title, body; vertical timeline visual; `<h2>` heading
- [ ] 4.5 Create `src/components/landing/LPracticeAreas.astro` — import `{ practiceAreas }`; each area as `<article>` with `<h3>` + description `<p>`; section `<h2>`; standard layout, no hover animations
- [ ] 4.6 Create `src/components/landing/LClients.astro` — import `{ testimonials, clientLogos }` + all 5 client images; full testimonial text; `<Image />` logos with NO grayscale (color, full opacity); same caption text
- [ ] 4.7 Create `src/components/landing/LTeam.astro` — import `{ Image }`, `{ team }` + all 5 lawyer images; `id="equipo"`; each member: `<Image />` photo, `<h3>` name, role, bio `<p>`, credentials `<ul>`; use existing credentials array; add `{/* TODO: extended bio from client */}` comments
- [ ] 4.8 Create `src/components/landing/LContact.astro` — import `ContactForm` + `{ contact }`; `id="contacto"`; same border-left info block as S07; section `<h2>`; standard scrollable layout
- [ ] 4.9 Create `src/components/landing/LFooter.astro` — copyright "© 2026 A un toque legal — Cisternas, Lorenzini & Saleh." Montserrat Light 300 `0.7rem` `#807662` centered
- [ ] 4.10 Create `src/pages/landing.astro` — import BaseLayout (robots="index, follow", canonical="https://auntoquelegal.cl/landing", full title/description from siteMeta); JSON-LD Organization+LegalService in `<head>` slot; import TopBannerBack + all L* components; `<main id="main" class="pt-10">` with all sections in order; zero Motion One JS

## Phase 5: SEO Infrastructure

- [ ] 5.1 Create `src/pages/sitemap.xml.ts` — `export const GET: APIRoute` returns XML with single `<url>` for `https://auntoquelegal.cl/landing`, changefreq monthly, priority 1.0; Content-Type `application/xml; charset=utf-8`
- [ ] 5.2 Update `public/robots.txt` — replace contents: `User-agent: *` / `Allow: /landing` / `Disallow: /` / blank line / `Sitemap: https://auntoquelegal.cl/sitemap.xml`

## Phase 6: Cleanup

- [ ] 6.1 Delete `src/components/sections/` entire directory (Nav, Hero, Diferenciadores, ComoTrabajamos, Areas, Clientes, Equipo, Contacto, Footer) — fully replaced by fullpage/ and landing/ trees
- [ ] 6.2 Audit remaining `.astro` and `.ts` files for any remaining `@fontsource-variable/*` imports; remove any found

## Phase 7: Verification

- [ ] 7.1 Run `npx astro check` — must complete with 0 TypeScript errors
- [ ] 7.2 Run `npm run build` — must complete with 0 errors; verify `/` and `/landing` routes generated, sitemap.xml included
- [ ] 7.3 Verify acceptance criteria from spec.md §6 — noindex on `/`, index+canonical on `/landing`, sitemap XML only has `/landing`, zero external font requests, all 9 Montserrat weights load, fullpage navigation via click/keyboard/swipe, counter updates, progress bar fills, hint fades after 3s, 800ms debounce works, S06 carousel doesn't trigger section nav, all tap targets ≥44px, no horizontal overflow at 390px, JSON-LD in landing head
